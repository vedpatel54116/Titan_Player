import SwiftUI

struct PlayerView: View {
    var body: some View {
        ZStack {
            Color.black
            Text("No media loaded")
                .foregroundColor(.white)
        }
    }
}
